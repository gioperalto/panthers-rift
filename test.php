<?php

	// require 'tools/PHPMailerAutoload.php';

	// $mail = new PHPMailer;

	// // $mail->SMTPDebug = 1;                               // Enable verbose debug output

	// $mail->isSMTP();                                      // Set mailer to use SMTP
	// $mail->Host = 'smtp.gmail.com';  					  // Specify main and backup SMTP servers
	// $mail->SMTPAuth = true;                               // Enable SMTP authentication
	// $mail->Username = 'panthersrift@gmail.com';                 // SMTP username
	// $mail->Password = '36g7xcy7y';                           // SMTP password
	// $mail->SMTPSecure = 'tls';                            // Enable TLS encryption, `ssl` also accepted
	// $mail->Port = 587;                                    // TCP port to connect to

	// $mail->From = 'admin@panthersrift.com';
	// $mail->FromName = 'Panther\'s Rift';
	// $mail->addAddress('gyoperalto@gmail.com', 'Gio Peralto-Pritchard');     // Add a recipient
	// // $mail->addAddress('ellen@example.com');               // Name is optional
	// $mail->addReplyTo('admin@panthersrift.com', 'Panther\'s Rift');
	// // $mail->addCC('cc@example.com');
	// // $mail->addBCC('bcc@example.com');

	// // $mail->addAttachment('/var/tmp/file.tar.gz');         // Add attachments
	// // $mail->addAttachment('/tmp/image.jpg', 'new.jpg');    // Optional name
	// $mail->isHTML(true);                                  // Set email format to HTML

	// $mail->Subject = 'Here is the subject';
	// $mail->Body    = 'This is the HTML message body <b>in bold!</b>';
	// $mail->AltBody = 'This is the body in plain text for non-HTML mail clients';

	// if(!$mail->send()) {
	//     echo 'Message could not be sent.';
	//     echo 'Mailer Error: ' . $mail->ErrorInfo;
	// } else {
	//     echo 'Message has been sent';
	// }	

	// require_once 'tools/emailer.php';
	
	// sendRegistrationEmail('Gio Peralto-Pritchard','gyoperalto@gmail.com','1');

	// sendSponsorEmail(
	// 'Gio Peralto-Pritchard',
	// 'gpera008@fiu.edu',
	// 'oneironautgames@gmail.com',
	// 'League of Legends Tournament',
	// 'Greetings Oneironaut Games,

	// I am contacting you on behalf of the Panther\'s Rift, which will be taking place on March 7th and 21st at FIU, and would like
	// to know if you would be interested in a potential sponsorship opportunity with our tournament. We will be gathering hundreds of
	// students from the Miami area together for a huge networking community event.

	// This is an opportunity to let the gaming community know who you are, and by supporting through our modest sponsorship tiers, you will
	// make a significant impact on the success and reach of this tournament.

	// I wish a great day to you all, and I look forward to hearing from you soon!'
	// );

?>